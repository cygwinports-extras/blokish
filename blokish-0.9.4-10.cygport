WX_VERSION=2.8
WX_TOOLKIT=gtk2
inherit wxwidgets

HOMEPAGE="Blokus game with AI"
SRC_URI="mirror://sourceforge/${PN}/${PN}_v${PV}.tgz"
PATCH_URI="mirror://portage/games-board/${PN}/files/${P}-gcc43.patch
           0.9.4-wx-config.patch"
SRC_DIR="${PN}"

CYGCONF_ARGS="--with-wx-config=${WX_CONFIG}"

src_install() {
	cd ${B}
	cyginstall

	doicon ${S}/src/blokish.xpm
	make_desktop_entry blokish Blokish blokish "Game;LogicGame"
}
